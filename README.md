# ResumeRAG
Built a Retrieval-Augmented Generation (RAG) system from scratch using Python. Extracts text from PDFs, splits it into chunks, generates embeddings with Sentence Transformers, stores them in FAISS, retrieves relevant context, and uses an LLM to answer user queries.
